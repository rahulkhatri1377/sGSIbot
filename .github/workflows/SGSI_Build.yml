name: SGSI_Build

on:
  push:
  workflow_dispatch:

env:              
  ROM_URL: 'https://mirrorxs.sharukh007.workers.dev/0://G820NO30d_00_OPEN_KR_OP_1119.kdz'
jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - name: Get the source code of this repositorie...
        uses: actions/checkout@main

      - name: Get variable configuration...
        run: |
          echo "BUILD_TIME=$(date +%s | md5sum | awk '{print substr($1,1,10)}')" >> $GITHUB_ENV ; echo "$REPACK_NAME"

      - name: Clean up the environment‌‌...
        run: |
          sudo -E apt-get clean
          sudo apt install -y unace unrar zip unzip p7zip-full p7zip-rar sharutils rar uudeview mpack arj cabextract rename liblzma-dev brotli lz4
          sudo apt update

      - name: Get the SGSI tool‌‌...
        run: |
          git clone --recurse-submodules https://github.com/erfanoabdi/Firmware_extractor fe

      - name: Configuration tool environment...
        run: |
          pip install backports.lzma protobuf pycrypto

      - name: Make AB type GSI‌‌...
        run: |
          cd fe ; aria2c -x16 -j$(nproc) -U "Mozilla/5.0" $ROM_URL
          sudo bash extractor.sh G*.kdz ; ls

      - name: Repack output file ...
        run: |            
             sudo chmod -R 777 $GITHUB_WORKSPACE/SGSI-build-tool/SGSI
             cd $GITHUB_WORKSPACE/SGSI-build-tool/SGSI
             ls
             sudo chmod -R 777 system.img
             gzip -cfStv9 *img > $REPACK_NAME.img.gz

      - name: Upload SGSI to SourceForge
        continue-on-error: true
        run: |
             cd $GITHUB_WORKSPACE/SGSI-build-tool/SGSI
             ls
             expect -c "
             spawn sftp ${{ secrets.SFUSER }}@frs.sourceforge.net
             expect \"yes/no\"
             send \"yes\r\"
             expect \"Password\"        
             send \"${{ secrets.SFPASS }}\r\"
             expect \"sftp> \"
             send \"cd ${{ secrets.SFDIR }}\r\"
             set timeout -1
             send \"mkdir $REPACK_NAME\r\"
             expect \"sftp> \"
             send \"cd $REPACK_NAME\r\"
             set timeout -1
             send \"put *\r\"
             expect \"Uploading\"
             expect \"100%\"
             expect \"sftp>\"
             send \"bye\r\"
             interact"
